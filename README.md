# FocusShell (scaffold)

This repository contains a starter scaffold for FocusShell — an Android-first distraction-minimizing launcher built with Expo + React Native, ready to migrate to Bare/EAS for native modules.

Run the scaffold creation script to populate file structure and starter files.
